name = "soulclient"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Public (non-secret) env vars
[vars]
DISCORD_CLIENT_ID = "1492641149494755370"
SUPABASE_URL = "https://tryqwbidrcmdhkyllxti.supabase.co"
SUPABASE_PUBLISHABLE_KEY = "sb_publishable_f-pNX3Wp-nBVXV2T7oJbHA_BGBCIdC7"
SUPABASE_BUCKET = "instances"

# The instance ZIPs live in a public Supabase Storage bucket (SUPABASE_BUCKET).
# Create it once from the Supabase Dashboard (Storage -> New bucket, public) or
# with the service-role key via the Storage API.

# Secrets (set via CLI – NEVER commit these):
#   wrangler secret put DISCORD_CLIENT_SECRET
#   wrangler secret put SUPABASE_SECRET_KEY
#   wrangler secret put PUBLISH_TOKEN
